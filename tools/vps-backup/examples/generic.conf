# Generic profile template — copy and edit
# sudo bash backup.sh --mode smart --profile examples/generic.conf

# Space-separated absolute paths to always include
EXTRA_PATHS="/etc/nginx /etc/wireguard /opt"

# Docker compose files (space-separated)
DOCKER_COMPOSE_FILES=""

# CUSTOM_PRE_HOOK=
# CUSTOM_POST_HOOK=
